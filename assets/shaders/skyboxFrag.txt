#version 330 core
out vec4 FragColor;

in vec3 TexCoords;
uniform samplerCube skybox;
uniform vec3 skyColour = vec3(0.0f, 0.0f, 1.0f);

void main(){    
    FragColor = texture(skybox, TexCoords) * vec4(skyColour, 1.0f);
}